# 仓库管理系统 - 脚本使用说明

## 📋 脚本概览

本项目提供了简化的脚本管理系统，每种功能只有一个脚本：

| 脚本 | 说明 | 用途 |
|------|------|------|
| `install.sh` | 安装脚本 | 安装所有依赖（后端+前端） |
| `start.sh` | 启动脚本 | 启动服务（支持开发/生产模式） |
| `stop.sh` | 停止脚本 | 停止所有运行中的服务 |
| `status.sh` | 状态查看 | 查看服务运行状态 |
| `logs.sh` | 日志查看 | 实时查看日志输出 |

## 🚀 快速开始

### 1. 安装依赖

首次使用需要安装依赖：

```bash
./install.sh
```

**安装内容：**
- Python 虚拟环境
- 后端依赖（Flask、flask-cors 等）
- 前端依赖（Vue、Vite 等）
- 创建必要的目录

### 2. 启动服务

#### 开发模式（推荐用于开发）

```bash
./start.sh dev
```

**特点：**
- ✅ 前端运行在 `http://localhost:3000`（Vite 开发服务器）
- ✅ 后端运行在 `http://localhost:8080`（Flask API）
- ✅ 支持前端热更新（修改代码自动刷新）
- ✅ 前端自动代理 API 请求到后端
- ✅ 适合开发调试

**访问地址：**
- 👉 **http://localhost:3000** （推荐访问此地址）

#### 生产模式（推荐用于部署）

```bash
./start.sh prod
```

**特点：**
- ✅ 自动构建前端（生成优化后的静态文件）
- ✅ 前端文件输出到 `static/` 目录
- ✅ 只运行 Flask 后端，同时服务前端静态文件
- ✅ 性能优化，适合生产部署

**访问地址：**
- 👉 **http://localhost:8080**

### 3. 停止服务

```bash
./stop.sh
```

**功能：**
- 自动检测并停止所有运行中的服务
- 支持开发模式和生产模式
- 清理 PID 文件

### 4. 查看状态

```bash
./status.sh
```

**显示信息：**
- 后端服务状态（端口 8080）
- 前端服务状态（端口 3000）
- 进程 ID 和资源占用
- 运行模式（开发/生产）
- 访问地址
- 最近日志

### 5. 查看日志

```bash
./logs.sh
```

实时查看最新的日志输出（按 Ctrl+C 退出）

## 📖 使用场景

### 场景 1：本地开发

```bash
# 1. 首次安装
./install.sh

# 2. 启动开发环境
./start.sh dev

# 3. 访问 http://localhost:3000 进行开发

# 4. 修改代码，浏览器自动刷新

# 5. 完成后停止
./stop.sh
```

### 场景 2：生产构建

```bash
# 1. 构建并启动生产环境
./start.sh prod

# 2. 访问 http://localhost:8080 验证

# 3. 验证完成后停止
./stop.sh
```

### 场景 3：检查服务状态

```bash
# 查看当前运行状态
./status.sh

# 查看实时日志
./logs.sh
```

### 场景 4：切换模式

```bash
# 从开发模式切换到生产模式
./stop.sh              # 先停止当前服务
./start.sh prod        # 启动生产模式

# 从生产模式切换到开发模式
./stop.sh              # 先停止当前服务
./start.sh dev         # 启动开发模式
```

## 🔧 命令参数说明

### start.sh

```bash
./start.sh [模式]
```

**模式选项：**
- `dev` - 开发模式（前后端分离）
- `prod` - 生产模式（构建前端）

**示例：**
```bash
./start.sh dev    # 启动开发环境
./start.sh prod   # 启动生产环境
./start.sh        # 显示使用说明
```

## 🌐 端口说明

| 端口 | 服务 | 模式 |
|------|------|------|
| 8080 | Flask 后端 | 所有模式 |
| 3000 | Vite 前端 | 仅开发模式 |

## 📁 日志文件

日志文件保存在 `logs/` 目录：

| 文件名 | 说明 |
|--------|------|
| `backend_YYYYMMDD_HHMMSS.log` | 开发模式后端日志 |
| `frontend_YYYYMMDD_HHMMSS.log` | 开发模式前端日志 |
| `app_YYYYMMDD_HHMMSS.log` | 生产模式日志 |

## ❓ 常见问题

### 1. 端口被占用

**错误信息：**
```
⚠️  警告: 端口 8080 已被占用
```

**解决方法：**
```bash
# 停止所有服务
./stop.sh

# 或手动终止进程
lsof -ti:8080 | xargs kill -9
lsof -ti:3000 | xargs kill -9
```

### 2. 前端依赖未安装

**错误信息：**
```
❌ 错误: 前端依赖未安装
```

**解决方法：**
```bash
cd frontend
npm install
cd ..
```

### 3. 虚拟环境不存在

**错误信息：**
```
❌ 错误: 虚拟环境不存在
```

**解决方法：**
```bash
./install.sh
```

### 4. flask-cors 未安装

启动脚本会自动检测并安装 flask-cors，如果失败：

```bash
source venv/bin/activate
pip install flask-cors
```

### 5. 查看详细错误

```bash
# 查看后端日志
tail -f logs/backend_*.log

# 查看前端日志
tail -f logs/frontend_*.log

# 或使用日志脚本
./logs.sh
```

### 6. 重新安装

如果遇到依赖问题，可以重新安装：

```bash
# 删除虚拟环境和前端依赖
rm -rf venv frontend/node_modules

# 重新安装
./install.sh
```

## 🔐 默认登录账号

- **用户名：** admin
- **密码：** admin123

⚠️ **首次登录后请立即修改密码！**

## 🛠️ 技术栈

### 后端
- Python 3.8+
- Flask 3.0
- Flask-CORS
- SQLite

### 前端（开发模式）
- Vue 3
- Vite 5
- Vue Router 4
- Axios

### 前端（生产模式）
- 构建后的静态文件
- 由 Flask 直接服务

## 💡 开发建议

1. **日常开发**：使用 `./start.sh dev`，享受热更新
2. **生产部署**：使用 `./start.sh prod`，验证生产构建
3. **定期检查**：使用 `./status.sh` 查看服务状态
4. **调试问题**：使用 `./logs.sh` 查看实时日志
5. **切换模式**：先 `./stop.sh` 再启动新模式

## 📚 更多文档

- 前端开发文档：`frontend/README.md`
- 系统功能说明：`系统功能总结.md`
- 部署说明：`DEPLOY.md`

## 🎯 脚本设计原则

1. **简单易用**：每种功能只有一个脚本
2. **智能检测**：自动检测环境和依赖
3. **友好提示**：清晰的错误信息和解决建议
4. **安全可靠**：启动前检查端口占用
5. **灵活切换**：支持开发和生产模式

## 🔄 版本历史

- **v2.0** - 简化脚本结构，统一启动/停止脚本
- **v1.0** - 初始版本，分离的启动脚本
